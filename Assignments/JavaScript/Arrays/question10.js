// Create a function that returns the first n elements of an Create a function that returns the first n elements of an array

let arr = [1, 2, 3, 4, 5];

function array() {
  for (let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
  }
}

array();
